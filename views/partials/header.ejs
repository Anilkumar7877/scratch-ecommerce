<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <nav class="flex justify-between px-20 text-lg items-center h-20">
        <h3 class="text-2xl text-blue-500 font-semibold">Scratch</h3>
        <% var loggedin = (typeof loggedin !=="undefined") ? loggedin : true %>
        <% if(loggedin){ %>
            <div class="flex gap-12">
                <a href="/shop">Shop</a>
                <a href="/cart">Cart</a>
                <a href="">My Account</a>
                <a href="/users/logout" class="text-red-500">Logout</a>
            </div>
        <% } %>
    </nav>
</body>
</html>